rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // ============================================
    // DEVELOPMENT RULES (Temporary - for testing)
    // ============================================
    // WARNING: These rules allow full read/write access!
    // Use only during development. Update for production.
    
    // Allow read/write access to all documents during development
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
